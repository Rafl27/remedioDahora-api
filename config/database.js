module.exports = {
    // replace the <username>, <password>, <cluster-name> and <database-name> placeholders with your own values
    mongoURI: `mongodb+srv://<username>:<password>@<cluster-name>.mongodb.net/<database-name>?retryWrites=true&w=majority`
  }
  